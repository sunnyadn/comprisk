repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.11
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
        exclude: '\.patch$'
      - id: end-of-file-fixer
        exclude: '\.patch$'
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        # The 500 KB ceiling guards against committing cohort data or binary
        # blobs; keep it tight and exempt the one legitimate large text file
        # rather than raising it for everything.
        args: [--maxkb=500]
        exclude: ^uv\.lock$
      - id: check-merge-conflict
      - id: mixed-line-ending
      - id: detect-private-key
      - id: debug-statements
      - id: check-case-conflict
